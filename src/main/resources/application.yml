spring:
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
#  datasource:
#    url: jdbc:h2:tcp://localhost/~/hachishop
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
      dialect : org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
        show_sql: true
        format_sql: true

  logging:
    level:
      org.hibernate.SQL: debug
      org.hibernate.type: trace